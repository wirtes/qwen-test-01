version: '3.8'

services:
  qwen-lite:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./cache:/root/.cache/huggingface  # Cache models locally
    restart: unless-stopped